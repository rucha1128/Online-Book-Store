@model List<Categories>

<div class="row pt-4">
    <div class="col-6">
        <h1 class="text-primary">Categories</h1>
    </div>
    <div class="col-6 text-end">
        <a asp-controller="Categories" asp-action="AddCategory" class="btn btn-dark">
        <i class="bi bi-plus"></i> Add Category</a>
    </div>
    <table class="table table-boarder table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Display Order</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var o in Model)
            {
                <tr>
                    <td>@o.Name</td>
                    <td>@o.DisplayOrder</td>
                    <td>
                        <a asp-controller="Categories" asp-action="Edit" asp-route-id="id" class="btn btn-dark"><i class="bi bi-pencil-square"></i>Edit</a>
                        <a asp-controller="Categories" asp-action="Delete" class="btn btn-danger"><i class="bi bi-trash"></i>Delete</a>
                    </td>
                    
                </tr>
            }
        </tbody>
    </table>
</div>
